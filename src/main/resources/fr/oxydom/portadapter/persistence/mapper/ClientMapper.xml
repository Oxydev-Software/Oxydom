<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="fr.oxydom.portadapter.persistence.mapper.ClientMapper">
    <select id="retrieveById" parameterType="Int" resultType="fr.oxydom.portadapter.persistence.entity.EClient">
        SELECT * FROM t_client_clt WHERE id = #{id}
    </select>
</mapper>