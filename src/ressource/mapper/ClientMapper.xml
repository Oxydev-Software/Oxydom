<?xml version="1.0" encoding="UTF-8" ?>

<mapper>
    <select id="retrieveById" parameterType="int" resultType="portadapter.persistence.entity.EClient">
        SELECT * FROM client WHERE id = #{id}
    </select>
</mapper>